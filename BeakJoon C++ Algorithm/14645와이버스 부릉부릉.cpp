/// 14645와이버스 부릉부릉

#include <iostream>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	int n, k, a, b;
	cin >> n >> k;
	while (n--) {
		for (int i = 0; i < k; i++)
			cin >> a >> b;
	}
	cout << "비와이\n";
}